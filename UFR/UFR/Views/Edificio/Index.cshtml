@model IEnumerable<UFR.Models.Edificio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descricao.Valor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descricao.Opcoes)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao.Texto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao.Valor)
        </td>
        <td>
            @foreach (var option in item.Descricao.Opcoes)
            {
            <p>@Html.DisplayFor(m => option)</p>
                
            }

        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.Id  }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", routeValues: new { id = item.Id })
        </td>
    </tr>
    }

</table>
